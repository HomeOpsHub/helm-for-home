apiVersion: v1
kind: Service
metadata:
  name: {{ include "ispyagentdvr.fullname" . }}
  labels:
    {{- include "ispyagentdvr.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ispyagentdvr.selectorLabels" . | nindent 4 }}
