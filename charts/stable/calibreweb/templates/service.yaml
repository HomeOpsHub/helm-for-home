apiVersion: v1
kind: Service
metadata:
  name: {{ include "calibreweb.fullname" . }}
  labels:
    {{- include "calibreweb.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "calibreweb.selectorLabels" . | nindent 4 }}
