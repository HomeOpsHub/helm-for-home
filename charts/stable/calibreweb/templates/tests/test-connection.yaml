apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "calibreweb.fullname" . }}-test-connection"
  labels:
    {{- include "calibreweb.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "calibreweb.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
