Thank you for installing {{ .Chart.Name }} v{{ .Chart.AppVersion }}!

  Access the Web UI:
    kubectl --namespace {{ .Release.Namespace }} port-forward svc/{{ include "code-server.fullname" . }} 8443:{{ .Values.service.port }}
    Web UI: http://localhost:8443

  Check pod status:
    kubectl get pods --namespace {{ .Release.Namespace }} -l app.kubernetes.io/instance={{ .Release.Name }}

  View logs:
    kubectl logs --namespace {{ .Release.Namespace }} -l app.kubernetes.io/instance={{ .Release.Name }}

  Docs: https://coder.com
