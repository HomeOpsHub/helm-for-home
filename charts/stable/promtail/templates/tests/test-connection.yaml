apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "promtail.fullname" . }}-test-connection"
  labels:
    {{- include "promtail.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "promtail.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
