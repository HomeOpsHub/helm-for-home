apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "get-iplayer.fullname" . }}-test-connection"
  labels:
    {{- include "get-iplayer.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "get-iplayer.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
