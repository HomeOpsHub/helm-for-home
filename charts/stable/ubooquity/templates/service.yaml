apiVersion: v1
kind: Service
metadata:
  name: {{ include "ubooquity.fullname" . }}
  labels:
    {{- include "ubooquity.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ubooquity.selectorLabels" . | nindent 4 }}
