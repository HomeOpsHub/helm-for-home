apiVersion: v1
kind: Service
metadata:
  name: {{ include "krusader.fullname" . }}
  labels:
    {{- include "krusader.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "krusader.selectorLabels" . | nindent 4 }}
