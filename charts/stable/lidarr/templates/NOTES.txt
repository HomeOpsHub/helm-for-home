Thank you for installing {{ .Chart.Name }} v{{ .Chart.AppVersion }}!

Lidarr has been deployed successfully.

  Access the Web UI:
    kubectl --namespace {{ .Release.Namespace }} port-forward svc/{{- include "lidarr.fullname" . }} 8686:{{ .Values.service.port }}
    Web UI: http://localhost:8686

  Check pod status:
    kubectl get pods --namespace {{ .Release.Namespace }} -l app.kubernetes.io/instance={{ .Release.Name }}

  View logs:
    kubectl logs --namespace {{ .Release.Namespace }} -l app.kubernetes.io/instance={{ .Release.Name }}

  Docs: https://lidarr.audio
