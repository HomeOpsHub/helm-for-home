apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "drone-ci.fullname" . }}-test-connection"
  labels:
    {{- include "drone-ci.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "drone-ci.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
