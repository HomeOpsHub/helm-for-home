apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "nomad.fullname" . }}-test-connection"
  labels:
    {{- include "nomad.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "nomad.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
